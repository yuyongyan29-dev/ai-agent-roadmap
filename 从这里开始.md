# 从这里开始

这份文件回答一个问题：**打开 VS Code 之后，具体第一步做什么。**

---

## 一、今天的 60 分钟（做完就算正式开始）

### 第 1 步：装 VS Code（10 分钟）

去 [code.visualstudio.com](https://code.visualstudio.com/) 下载安装。

### 第 2 步：用 VS Code 打开这个仓库（1 分钟）

菜单 `File → Open Folder`，选桌面上的 `AI-Agent-学习计划` 文件夹。

打开后 VS Code 右下角会弹「此工作区有推荐的扩展」，**点「安装」**。这会装上仓库里 `.vscode/extensions.json` 预设的 20 个插件，包括中文界面包、Python 全套、Git 增强、Docker、数据库客户端。不弹的话按 `Cmd+Shift+X` 打开扩展面板，搜索框输入 `@recommended`，逐个装。

装完重启 VS Code，界面变中文。

### 第 3 步：认识三个面板（10 分钟）

只需要先认识这三个，其余以后自然会用到。

| 快捷键 | 面板 | 什么时候用 |
|---|---|---|
| `Cmd+Shift+E` | 资源管理器 | 看文件树，找文件 |
| `` Ctrl+` `` | 终端 | **你 90% 的时间在这里**，跑命令、看报错 |
| `Cmd+Shift+G` | 源代码管理 | 看改了什么、提交、推送 |

再记两个：`Cmd+P` 按文件名跳转，`Cmd+Shift+F` 全局搜索。

### 第 4 步：打开第一个单元（5 分钟）

`Cmd+P`，输入 `s0`，回车打开 `units/s0-工程地基.md`。

按 `Cmd+Shift+V` 打开 Markdown 预览。**建议左边放单元文档，右边放终端**，边看边做。

### 第 5 步：开始 U0.1（剩下的时间）

从「U0.1 命令行与开发环境」的第 1 条开始，一条一条往下做。

---

## 二、每个单元的固定五步

**这个循环重复 54 次，26 周就走完了。** 不要发明新流程，照这五步走。

**① 读**（15 分钟）
打开单元文档，先把整个单元通读一遍，**特别是最后的「完成判据」**。你要带着「我最后要交出什么」的目标开始，而不是读一步做一步。

**② 建目录**
在仓库里建这个单元的工作目录，比如 `s0-foundation/`。复制 `templates/单元笔记模板.md` 到目录里改名成 `U0.1-笔记.md`。

**③ 做**（这个单元的绝大部分时间）
按文档里的编号步骤逐条执行。每完成一条，在笔记里记一句你实际做了什么——**不是抄步骤，是写你实际遇到了什么**。

**④ 验**
对照「完成判据」逐条检查。**判据没达到就不算完成**，宁可多花半天。在笔记的判据表里填上证据（命令输出、截图路径、文件路径）。

**⑤ 交**
```bash
git add -A
git commit -m "U0.1: 完成命令行与开发环境"
git push
```
然后回 `README.md` 的进度表填上这一行，回单元文档把 `- [ ]` 改成 `- [x]`。

---

## 三、每天怎么安排

每周 20 小时，建议这么切：

| 时段 | 时长 | 做什么 |
|---|---|---|
| 工作日每天晚上 | 2h | 一个时间盒，只推进当前单元 |
| 周六 | 5h | 大块时间，啃硬骨头（通常是当周最难的那个单元） |
| 周日上午 | 3h | 补齐判据、补测试 |
| 周日下午 | 2h | **写周复盘**，复制 `templates/周复盘模板.md` 到 `logs/2026-Wxx.md` |

**周复盘是唯一不能省的一项。** 它是把「做过」变成「会讲」的地方，26 周后你的面试素材全在这里。

---

## 四、怎么用 AI 辅助而不被它代写

这是零基础学编程最大的风险。**规矩是：S0 和 S1 阶段，AI 只能解释，不能代写。**

VS Code 里装了 Claude Code 插件，按 `Cmd+Esc` 唤起。或者在终端里直接输 `claude`。

**好的提问方式**（用这些）

```
这段报错是什么意思？逐行解释给我听，不要直接给我改好的代码。
  <贴报错>

我想实现「读 CSV 按列分组」，我的思路是先用 open() 再 split()。
这个思路有什么问题？给我方向，不要给我完整代码。

解释一下 `with open(...) as f:` 里的 with 是干什么的，
不用 with 会怎么样？给我一个能自己跑出区别的小例子。

review 一下我这段代码，指出哪里写得不像 Python，说明为什么。
  <贴代码>
```

**坏的提问方式**（S0/S1 阶段别用）

```
帮我写一个读 CSV 的脚本      ← 你会得到答案，学不到东西
这个 bug 帮我修一下           ← 修完你还是不知道为什么错
```

**分界线在 S2。** 从 S2 开始你可以放开用 AI 加速写代码，因为那时你已经能判断它写得对不对。但架构决策——用不用多 Agent、选哪个框架、怎么切分工具——**必须你自己做并写清楚理由**，这些正是面试要问的。

---

## 五、卡住了怎么办

按这个顺序升级，**不要跳级**：

1. **自己撞 30 分钟**。读报错的最后一行，Google 报错原文，看官方文档。这 30 分钟是必要的，你的调试能力就是这么长出来的。
2. **问 AI 要解释**，用上面的提问模板。
3. **降低范围**。写一个 5 行的最小复现，把无关的东西全删掉。**很多时候删到一半你就自己发现问题了。**
4. **跳过，标记，继续**。在笔记里写「TODO: 这里没搞懂 xxx」，先往下走。Todo Tree 插件会把所有 TODO 收集到侧边栏，下次复盘时回来清。
5. **一个单元卡超过 2 天**，说明前置知识缺了。回头看这个单元依赖哪个前置单元，补那个。

---

## 六、前三天的具体清单

不用想，照做。

**第 1 天（2h）**
- [ ] 装 VS Code + 推荐插件，界面变中文
- [ ] 用 VS Code 打开本仓库
- [ ] U0.1 第 1-3 条：装 Homebrew，`brew install git gh uv`
- [ ] 建 `s0-foundation/` 目录，复制单元笔记模板

**第 2 天（2h）**
- [ ] U0.1 第 4-7 条：练命令、理解路径、管道重定向、看懂 `.zshrc`
- [ ] 达成 U0.1 判据：纯命令行完成那一串操作
- [ ] commit + push，勾掉 U0.1

**第 3 天（2h）**
- [ ] U0.2 第 1-3 条：配 git 身份，走一遍 add/commit/push，每步跑 `git status`
- [ ] 玩 [Learn Git Branching](https://learngitbranching.js.org/?locale=zh_CN) 主线

**第一周结束时，你应该有**：一个装好工具的开发环境、一个有 5 个以上 commit 的 GitHub 仓库、一个能跑的 Python 脚本、一篇周复盘。

---

## 七、飞书文档在哪

手机上或者不在电脑前时查看：

| 文档 | 链接 |
|---|---|
| 总览 | https://dcnxpg5pruao.feishu.cn/docx/HEKGdkG7EofGpgx3NwScJt70nbf |
| 从这里开始 · 上手指南 | https://dcnxpg5pruao.feishu.cn/docx/JgSPd46vBobhH7xBLZkcYQvZnwb |
| S0 工程地基 | https://dcnxpg5pruao.feishu.cn/docx/PeEcdCPrUogtRxxM9sYcbBLbnsd |
| S1 LLM 基础与上下文工程 | https://dcnxpg5pruao.feishu.cn/docx/KEasd8eeIoABqNxT9WTcMyIwnAe |
| S2 Agent 核心 | https://dcnxpg5pruao.feishu.cn/docx/EW8mdQGhAoMyS2xqcO2cm18Qnxf |
| S3 评估与可观测性 | https://dcnxpg5pruao.feishu.cn/docx/ZggndoxVHoiUsSx4r33cPIr6nJg |
| S4 生产化 | https://dcnxpg5pruao.feishu.cn/docx/DG2Xdr7lqosKixxFUIBcXlwOnWb |
| S5 产品外壳 | https://dcnxpg5pruao.feishu.cn/docx/EK9tdBy6RogwNDxzyaqcHptTnmc |
| S6 平台基建与面试 | https://dcnxpg5pruao.feishu.cn/docx/WbomdNmbbo7eQNxv97WctQYLnsb |

飞书文档里的勾选框可以直接点，适合在手机上标记进度。**代码和判据证据以本地仓库为准**，飞书只是查看端。
