# U3.7 · 评估进 CI（12 小时）

**这个单元解决什么问题**

现在你的评估要手动跑。人会忘、会偷懒、会在赶时间时跳过。

**这个单元把评估变成一道自动的门禁：改坏了，合并不进去。**

**做完这一步，你的项目就从「个人作品」变成了「工程产物」。**

**学完你能做到**：提一个把 prompt 改差的 PR，CI 自动拦下来并指出是哪几条题退化了。

---

## 步骤 1 · 第一个 GitHub Actions

### 这一步在做什么

先跑通最简单的：PR 时自动跑 pytest。

### 怎么做

建 `.github/workflows/test.yml`：

```yaml
name: 测试

on:
  pull_request:          # 每个 PR 都跑，问题在合并前就暴露
  push:
    branches: [main]     # 合并后再跑一次，防止两个 PR 单独没问题、合到一起出问题

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 装 uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true            # 缓存依赖，后面每次跑能省一两分钟

      - name: 装依赖
        # --frozen 严格按 uv.lock 安装：锁文件和 pyproject.toml 对不上就直接失败，
        # 保证 CI 装的版本和你本地一模一样，而不是悄悄升级出一个「只在 CI 挂」的环境
        run: uv sync --frozen

      - name: 代码检查
        run: |
          uv run ruff check .
          uv run ruff format --check .  # --check 只检查不改写，CI 里不该留下未提交的格式化改动

      - name: 单元测试
        run: uv run pytest -v --cov     # --cov 顺手出覆盖率，方便看哪些路径根本没被测到
```

提一个 PR，看它跑起来。

### 可能卡在哪

**`uv sync --frozen` 失败**

`uv.lock` 没提交，或者和 `pyproject.toml` 不一致。本地跑一次 `uv sync` 然后提交 lock 文件。

**测试需要数据库**

用 GitHub Actions 的 service 容器：

```yaml
    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_PASSWORD: test
        # 健康检查必须加：容器启动不等于数据库能接受连接，
        # 少了这段测试会连到还没就绪的库，表现为「偶发失败、重跑就好」
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-retries 10
        ports:
          - 5432:5432          # 映射到 runner 上，测试里按 localhost:5432 连
```

**测试要调模型 API**

**不应该。** U0.7 学的 mock 在这里发挥作用——单元测试必须全部 mock 掉外部调用。**真实调用留给评估（下一步）。**

### 背后的逻辑

CI（持续集成）的核心思想是：**每一次改动都自动验证，问题在最小的范围内被发现。**

对比没有 CI 的情况：你改了十个地方，一起提交，出问题了不知道是哪个改动导致的。

**这个思想在评估上同样成立**，只是验证的对象从「代码逻辑」变成了「模型行为」。

---

## 步骤 2 · 分层跑评估

### 这一步在做什么

**评估很贵很慢，不能每次 PR 都跑全量。**

### 三层策略

| 时机 | 跑什么 | 目标耗时 | 目标成本 |
|---|---|---|---|
| **每次 PR** | 标了 `core_path` 的子集（约 15 条），只用代码型评分器 | < 5 分钟 | < ¥1 |
| **合并到 main** | 全量（50 条），代码型 + LLM judge | < 20 分钟 | < ¥5 |
| **每晚定时** | 全量 + 成对比较 + 成本延迟报告 | 不限 | < ¥20 |

### 怎么实现

```yaml
name: 评估

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 18 * * *"        # UTC 18:00 = 北京时间凌晨 2:00

jobs:
  eval:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5

      - run: uv sync --frozen

      # 三档共用一个 workflow，靠触发事件区分跑多少：
      # 写成一步输出参数，而不是三个 job，避免同一套命令复制三遍以后改漏一处
      - name: 决定跑什么
        id: scope
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            # PR 只跑核心路径子集并关掉 LLM judge，把单次耗时压进 5 分钟、成本压到几毛
            echo "args=--tags core_path --no-judge" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            # 夜里没人等结果，跑最深的一档：全量 + 成对比较 + 成本报告
            echo "args=--all --judge --pairwise --report-cost" >> $GITHUB_OUTPUT
          else
            echo "args=--all --judge" >> $GITHUB_OUTPUT
          fi

      - name: 跑评估
        env:
          # 密钥只能从 secrets 注入，绝不能写进仓库；给 CI 用的 key 和本地开发的分开
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
          LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
          LANGFUSE_HOST: ${{ secrets.LANGFUSE_HOST }}
        run: |
          # 两份输出各有用处：Markdown 贴回 PR 给人看，JSON 交给门禁脚本和基线比对
          uv run python -m s3_eval.runner ${{ steps.scope.outputs.args }} \
            --baseline s3-eval/baseline.json \
            --output eval-report.md \
            --json eval-result.json

      - name: 上传报告
        if: always()          # 评估挂了才最需要看报告，所以成败都要传
        uses: actions/upload-artifact@v4
        with:
          name: eval-report
          path: |
            eval-report.md
            eval-result.json
```

### 可能卡在哪

**PR 的评估还是太慢**

再缩小子集。**10 条核心路径的题，只用规则评分，两三分钟就该跑完。**

**定时任务的 cron 时区**

GitHub Actions 用 UTC。北京时间减 8 小时。

**自托管的 Langfuse 在 CI 里连不上**

CI 跑在 GitHub 的机器上，连不到你本地的 Langfuse。三个选择：

1. CI 里不上报 trace（加个开关）
2. 把 Langfuse 部署到公网可访问的地方
3. CI 里也起一个临时的 Langfuse（service 容器）

**最简单是第 1 种**，CI 的评估结果存在 artifact 里就够了。

---

## 步骤 3 · 设门禁

### 这一步在做什么

**分数掉了就不让合并。**

### 基线机制

在仓库里存一个 `s3-eval/baseline.json`：

```json
{
  "updated_at": "2026-11-20T10:00:00Z",
  "commit": "a3f21b9",
  "full_pass_rate": 0.76,
  "core_path_pass_rate": 0.92,
  "per_case": {
    "EVAL-001": {"passed": true, "score": 1.0},
    "EVAL-002": {"passed": true, "score": 1.0},
    "EVAL-031": {"passed": false, "score": 0.67}
  }
}
```

**门禁规则**：

```python
def check_gate(current: dict, baseline: dict) -> tuple[bool, list[str]]:
    """把本次评估结果和基线比对，返回（是否放行, 问题列表）。

    问题列表会原样贴进 PR 评论，所以每条都写成人一眼能看懂的话，
    而不是只返回一个 True/False 让人再去翻 CI 日志。
    """
    problems = []

    # 规则 1：核心路径通过率不能低于基线
    if current["core_path_pass_rate"] < baseline["core_path_pass_rate"] - 0.02:   # 留 2% 容差，吸收 LLM 的随机波动
        problems.append(
            f"核心路径通过率从 {baseline['core_path_pass_rate']:.0%} "
            f"降到 {current['core_path_pass_rate']:.0%}"
        )

    # 规则 2：任何原本通过的题都不能变失败
    # 用 get 链而不是直接下标：题目可能这次被删掉或改了 id，取不到时按「没通过」算，
    # 宁可误报也不要因为一个 KeyError 让门禁整个崩掉
    for case_id, baseline_case in baseline["per_case"].items():
        if baseline_case["passed"] and not current["per_case"].get(case_id, {}).get("passed"):
            problems.append(f"{case_id} 原本通过，现在失败")

    return len(problems) == 0, problems
```

**规则 2 比规则 1 更严格也更有用。** 总通过率可能因为「修好 A 但改坏 B」而保持不变，规则 2 能抓到这种情况。

### 为什么要留容差

因为 LLM 有随机性。**同样的代码跑两次，分数可能差一两个百分点。**

**2% 是一个起点**，你可以先测一下：同样的 commit 连续跑 5 次，看分数的波动范围，容差设成波动范围的上限。

### 基线什么时候更新

**只在合并到 main 之后自动更新**：

```yaml
      # 只有 main 上跑绿了才把本次结果固化成新基线：
      # PR 分支上绝不能动基线，否则改坏的那次会顺手把标准也调低，门禁就形同虚设
      - name: 更新基线
        if: github.ref == 'refs/heads/main' && success()
        run: |
          cp eval-result.json s3-eval/baseline.json
          # 用机器人身份提交，历史里一眼能和真人改动区分开
          git config user.name "eval-bot"
          git config user.email "bot@localhost"
          git add s3-eval/baseline.json
          git commit -m "chore: 更新评估基线 [skip ci]"
          git push
```

**`[skip ci]` 很重要**，否则这次提交会触发新的 CI，无限循环。

### 可能卡在哪

**门禁太严，正常改动也被拦**

先观察一段时间再开启强制拦截。**建议流程**：

1. 第一周：只报告，不拦截
2. 观察波动范围，调整容差
3. 第二周开始强制拦截

**修改评估集本身导致基线失效**

改评估集时，基线里那些题的记录也要相应处理。**建议**：改评估集的 PR 单独提，合并后立刻重跑一次更新基线。

**紧急情况需要跳过门禁**

留一个逃生口：PR 标题带 `[skip-eval]` 或者打特定 label 时跳过。**但要在报告里明确标出「本次跳过了评估门禁」。**

---

## 步骤 4 · 结果贴回 PR

### 这一步在做什么

**让人在 PR 页面上直接看到影响，不用点进 CI 日志。**

### 怎么做

```yaml
      - name: 贴评论
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('eval-report.md', 'utf8');
            // 先把 PR 上已有的评论拉下来，找上一轮自己贴的那条
            const {data: comments} = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            // 用一行隐藏的 HTML 注释当标记：渲染出来看不见，但脚本能靠它认出自己的评论
            const existingComment = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes('<!-- eval-report -->')
            );
            const commentBody = '<!-- eval-report -->\n' + body;
            if (existingComment) {
              // 有旧评论就原地改写，PR 里始终只留最新一份结果
              await github.rest.issues.updateComment({
                owner: context.repo.owner, repo: context.repo.repo,
                comment_id: existingComment.id, body: commentBody,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: context.issue.number, body: commentBody,
              });
            }
```

**注意是「更新已有评论」而不是每次新增。** 否则一个 PR 改十次会有十条评论。

### 评论内容

```markdown
<!-- eval-report -->
## 评估结果 ❌

| 指标 | 本次 | 基线 | 变化 |
|---|---|---|---|
| 核心路径 | 12/15 (80%) | 14/15 (93%) | **-13%** ⚠️ |
| 成本 | ¥0.82 | ¥0.79 | +4% |
| 耗时 | 3m12s | 3m05s | +4% |

### ⚠️ 退化的题目（2 条）
- **EVAL-007** [核心路径] 原本通过，现在失败
  失败原因：不包含 · 输出里出现了禁词「家居」
  [查看 trace](链接)
- **EVAL-013** [核心路径] 原本通过，现在失败
  失败原因：轨迹 · 未调用「搜索笔记」工具

### 新通过的题目（0 条）

**门禁未通过。** 请修复退化的题目，或说明为什么这些退化是可接受的。
```

**「查看 trace」的链接**：如果 CI 上报了 trace 到公网可访问的 Langfuse，这个链接能直接跳过去看执行细节。**这是排查效率的关键。**

---

## 步骤 5 · 做一次验证演练

### 这一步在做什么

**证明你的门禁真的有用。**

### 怎么做

1. 开一个分支
2. **故意把 prompt 改差**。比如删掉「只回答家电类目相关的内容」这条约束
3. 提 PR
4. 等 CI 跑完
5. **确认它红灯，并且评论里准确指出了是哪几条题退化了**
6. 截图

### 如果没拦住

说明：

- 你改的那条约束没有对应的评估题（**评估集有盲区，去补**）
- 或者容差设太宽了
- 或者门禁规则有 bug

**每一种都是有价值的发现，修完再试一次。**

### 这个截图的价值

**这是 S3 最有说服力的作品集素材。**

面试时被问「你怎么保证 Agent 的质量」，你直接打开这张截图：一个改坏 prompt 的 PR，CI 自动拦下来，并且精确指出是哪两条评估题退化了、失败原因是什么。

**这比讲一万句「我很重视质量」都有用。**

---

## 步骤 6 · 密钥和成本管理

### 这一步在做什么

CI 里要调真实 API，涉及密钥和花钱。

### 密钥

存进 GitHub 仓库的 Settings → Secrets and variables → Actions。

**注意**：

- **fork 来的 PR 拿不到 secrets**（这是安全设计）。个人项目不受影响
- 定期轮换
- **给 CI 用单独的密钥**，和你本地开发的分开，这样出问题能单独作废

### 控制成本

**一、给 CI 的密钥设独立预算**。大多数厂商支持在控制台给某个 key 设上限。

**二、评估运行器里加总成本上限**：

```python
# 每跑完一题就检查累计花费，超了立刻用非 0 退出码中止：
# 熔断要在循环里做，等整轮跑完再算钱已经晚了
if total_cost > cost_limit:
    logger.error("评估成本超限，中止")
    sys.exit(2)
```

**三、并发控制**。CI 里也要用 Semaphore，别把限流撞了。

**四、监控趋势**。每次评估的成本记进报告，**如果发现成本在涨，说明上下文在膨胀**，该查了。

### 可能卡在哪

**CI 跑一次要几块钱，一天几十次 PR**

用分层策略（步骤 2）。PR 只跑 15 条规则评分，成本应该在几毛钱。

**忘了设上限，某次跑飞了**

**先设上限再开 CI。** 这个顺序不能反。

---

## 完成判据

**产出**：`.github/workflows/test.yml`、`.github/workflows/eval.yml`、`s3-eval/baseline.json`

**动手部分**

- [ ] pytest + ruff 在 PR 时自动跑
- [ ] **评估分三层**：PR 跑核心路径子集、main 跑全量、每晚跑深度
- [ ] PR 的评估在 5 分钟内完成，成本 1 元以内
- [ ] 基线机制，**合并到 main 后自动更新**（带 `[skip ci]`）
- [ ] 门禁两条规则：整体通过率不降、**原本通过的题不能变失败**
- [ ] 容差经过实测（同 commit 跑 5 次看波动）
- [ ] 结果贴回 PR 评论，**更新而不是新增**
- [ ] 评论里有退化题目的明细和失败原因
- [ ] CI 密钥独立、有预算上限、评估运行器有成本熔断

**关键验证：拦截演练**

提一个故意改差 prompt 的 PR，**确认 CI 红灯并准确指出退化的题目**。

**截图存进 README。** 这是你作品集里最有说服力的一张图。

**理解检查**（笔记里回答）

1. 为什么评估要分三层跑？
2. 门禁的两条规则里，为什么「原本通过的题不能变失败」比「整体通过率不降」更有用？
3. 容差为什么必须有？你测出来的波动范围是多少？
4. 基线什么时候更新？为什么更新基线的提交要带 `[skip ci]`？
5. 如果你的拦截演练没拦住，可能是哪三个原因？
