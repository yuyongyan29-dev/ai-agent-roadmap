# U5.4 · Vercel AI SDK 与流式界面（12 小时）

**这个单元解决什么问题**

**这是 Agent 产品体验的分水岭。**

现在的界面：用户点发送，转圈 30 秒，出结果。用户不知道它在干什么，也不知道要等多久，很可能中途就关掉了。

**目标**：用户看到「正在搜索…找到 23 条…正在分析…」这样的实时过程。

**版本基线**（2026-09-09 实测）：Vercel AI SDK（`ai` 包）7.0.95。**注意 v5 之后 API 有较大变化，照最新文档写，不要照老教程。**

**学完你能做到**：完整跑一次多步任务，全程能看到每一步；中途能停止；触发高危操作时弹确认。

---

## 步骤 1 · 理解数据从哪来

### 这一步在做什么

搞清楚整条链路，再动手。

### 链路

```
Agent 后端（S4 的 U4.3）
  │  产生事件写入 task_events 表
  │  通过 SSE 推送：GET /tasks/{id}/events
  ↓
Next.js API 路由（转发层）
  │  加鉴权，转发 SSE 流
  ↓
浏览器
  │  EventSource 或 fetch stream 接收
  ↓
React 状态 → 界面
```

**关键**：你在 U4.3 已经做好了事件流。**这个单元主要是把它接到界面上。**

### 事件类型回顾

```
任务开始 / 轮次开始 / 思考（流式文本片段）
工具开始 / 工具完成 / 工具失败
需要确认
任务完成 / 任务失败
```

**每种事件对应界面上的一种展示。**

---

## 步骤 2 · 转发 SSE

### 这一步在做什么

Next.js 的 API 路由把后端的 SSE 流转发给浏览器。

### 怎么做

```tsx
// src/app/api/tasks/[id]/events/route.ts
export const runtime = "nodejs";        // 不要用 edge，流式行为不一致

export async function GET(
  req: Request,
  { params }: { params: Promise<{ id: string }> },
) {
  const { id } = await params;
  const 用户 = await 取当前用户();
  if (!用户) return new Response("未授权", { status: 401 });

  // 归属校验：不能订阅别人的任务
  if (!(await 任务属于用户(id, 用户.id))) {
    return new Response("无权限", { status: 403 });
  }

  const lastEventId = req.headers.get("last-event-id") ?? "0";

  const 上游 = await fetch(
    `${process.env.AGENT_API}/tasks/${id}/events?last_event_id=${lastEventId}`,
    {
      headers: { Authorization: `Bearer ${process.env.AGENT_API_KEY}` },
      signal: req.signal,               // 客户端断开时也断开上游
    },
  );

  return new Response(上游.body, {
    headers: {
      "Content-Type": "text/event-stream",
      "Cache-Control": "no-cache, no-transform",
      "Connection": "keep-alive",
      "X-Accel-Buffering": "no",        // 关掉 nginx 缓冲
    },
  });
}
```

### 三个关键点

**一、`signal: req.signal`**。客户端关掉页面时，这个 signal 会被 abort，从而也断开对后端的连接。**不加的话后端连接会一直挂着，浪费资源。**

**二、`X-Accel-Buffering: no`**。很多反向代理会缓冲响应，导致流式变成一次性返回。这个头告诉它别缓冲。

**三、归属校验**。任务 id 是可猜的，必须验证这个任务属于当前用户。

### 可能卡在哪

**流式不生效，一次性全出来了**

按顺序排查：

1. `Content-Type` 是不是 `text/event-stream`
2. 有没有中间层缓冲（加 `X-Accel-Buffering: no`）
3. runtime 是不是 nodejs
4. 后端有没有真的流式发出来（用 `curl -N` 直接测后端）

**用 curl 测后端**是最有效的排查方法：

```bash
curl -N "http://后端地址/tasks/$ID/events?last_event_id=0"
```

**如果 curl 是流式的，问题在转发层；如果 curl 也是一次性的，问题在后端。**

---

## 步骤 3 · 前端接收事件

### 这一步在做什么

在 React 里把事件流变成状态。

### 用 EventSource（简单）

```tsx
"use client";
import { useEffect, useState, useRef } from "react";

type 事件 = { id: number; 类型: string; 数据: any };

function useTaskEvents(taskId: string | null) {
  const [事件们, 设事件们] = useState<事件[]>([]);
  const [状态, 设状态] = useState<"空闲" | "运行中" | "完成" | "失败">("空闲");
  const esRef = useRef<EventSource | null>(null);

  useEffect(() => {
    if (!taskId) return;

    const es = new EventSource(`/api/tasks/${taskId}/events`);
    esRef.current = es;
    设状态("运行中");

    const 处理 = (类型: string) => (e: MessageEvent) => {
      设事件们(前 => [...前, { id: Number(e.lastEventId), 类型, 数据: JSON.parse(e.data) }]);
    };

    ["轮次开始", "思考", "工具开始", "工具完成", "工具失败", "需要确认"]
      .forEach(t => es.addEventListener(t, 处理(t)));

    es.addEventListener("任务完成", (e) => {
      处理("任务完成")(e as MessageEvent);
      设状态("完成");
      es.close();
    });

    es.addEventListener("任务失败", (e) => {
      处理("任务失败")(e as MessageEvent);
      设状态("失败");
      es.close();
    });

    return () => { es.close(); };        // 清理，U5.2 学的
  }, [taskId]);

  const 停止 = () => {
    esRef.current?.close();
    fetch(`/api/tasks/${taskId}/cancel`, { method: "POST" });
  };

  return { 事件们, 状态, 停止 };
}
```

### EventSource 的优点和限制

| 优点 | 限制 |
|---|---|
| **浏览器内置，自动重连** | 只支持 GET |
| **自动带 Last-Event-ID** | **不能自定义请求头**（所以鉴权要靠 cookie） |
| 简单 | 不支持 POST |

**「自动带 Last-Event-ID」是关键优势**：断线重连时浏览器自动把上次收到的 id 发过去，配合你 U4.3 做的续传逻辑，**用户完全感知不到断线**。

**如果需要自定义头或 POST**，改用 `fetch` + `ReadableStream` 手动解析。**先用 EventSource 跑通，有需要再换。**

### 可能卡在哪

**重复连接**

`useEffect` 在开发模式下会执行两次。确保清理函数正确关闭连接。

**事件重复**

断线重连时如果 `last_event_id` 传得不对，会重复收到事件。**在状态更新时按 id 去重**：

```tsx
设事件们(前 => {
  if (前.some(e => e.id === 新事件.id)) return 前;
  return [...前, 新事件];
});
```

---

## 步骤 4 · 渲染执行过程

### 这一步在做什么

**这一步决定了你的界面是「Agent 产品」还是「聊天框」。**

### 要显示什么

```
┌─────────────────────────────────────┐
│ 用户：帮我看看这周家电类目值得做的选题   │
├─────────────────────────────────────┤
│                                     │
│  ⚙️ 正在搜索「家电 选题」…              │
│  ✓ 找到 23 条笔记（0.4 秒）            │
│                                     │
│  ⚙️ 正在读取 3 篇高赞笔记的详情…         │
│  ✓ 完成（1.2 秒）                     │
│                                     │
│  ⚙️ 正在分析互动数据…                   │
│  ✓ 完成                              │
│                                     │
│  💭 基于这些数据，我发现…（流式文本）      │
│                                     │
└─────────────────────────────────────┘
```

### 三条设计原则

**一、工具调用要说人话**

```tsx
// 差
工具：search_notes(keyword="家电 选题", limit=50)

// 好
正在搜索「家电 选题」…
```

**在事件的数据里带上给人看的描述**，而不是让前端去解析原始参数。

**二、完成的步骤要折叠但可展开**

任务跑到第 8 步时，前 7 步的详情占了太多空间。**默认折叠成一行摘要，点击展开看详情。**

**三、区分「进行中」和「已完成」**

用不同的图标和颜色。进行中的可以有动画（转圈或呼吸效果）。

### 组件结构

```tsx
function 执行过程({ 事件们 }: { 事件们: 事件[] }) {
  const 步骤 = 聚合成步骤(事件们);      // 把「工具开始」和「工具完成」配对

  return (
    <div className="space-y-2">
      {步骤.map(s => (
        <步骤条 key={s.id} 步骤={s} />
      ))}
    </div>
  );
}

function 步骤条({ 步骤 }: { 步骤: 步骤 }) {
  const [展开, 设展开] = useState(false);

  return (
    <div className="border-l-2 pl-3 py-1"
         style={{ borderColor: 步骤.状态 === "进行中" ? "#3b82f6" : "#d1d5db" }}>
      <button onClick={() => 设展开(!展开)} className="flex items-center gap-2 w-full text-left">
        <span>{步骤.状态 === "进行中" ? "⚙️" : 步骤.状态 === "失败" ? "❌" : "✓"}</span>
        <span className="flex-1">{步骤.描述}</span>
        {步骤.耗时 && <span className="text-xs text-gray-500">{步骤.耗时}s</span>}
      </button>
      {展开 && (
        <pre className="mt-2 text-xs bg-gray-50 dark:bg-gray-800 p-2 rounded overflow-x-auto">
          {JSON.stringify(步骤.详情, null, 2)}
        </pre>
      )}
    </div>
  );
}
```

### 可能卡在哪

**事件顺序乱了**

按事件 id 排序，不要依赖到达顺序。

**「工具开始」和「工具完成」怎么配对**

在事件数据里带一个 `调用id`，前端按它配对。**在后端产生事件时就带上。**

---

## 步骤 5 · 人工确认界面

### 这一步在做什么

把 U4.4 的确认流程接到界面上。

### 流程

```
收到「需要确认」事件
    ↓
显示确认卡片：操作摘要 + 影响范围 + 批准/拒绝按钮
    ↓
用户点击
    ↓
调用 /api/tasks/{id}/resume 带上决定
    ↓
后端继续执行，继续推事件
```

### 卡片设计

```tsx
function 确认卡片({ 确认, 任务id }: { 确认: 确认请求; 任务id: string }) {
  const [处理中, 设处理中] = useState(false);

  const 决定 = async (批准: boolean, 理由?: string) => {
    设处理中(true);
    await fetch(`/api/tasks/${任务id}/resume`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ 确认id: 确认.id, 批准, 理由 }),
    });
  };

  return (
    <div className="border-2 border-amber-400 rounded-lg p-4 bg-amber-50 dark:bg-amber-950">
      <div className="font-semibold mb-2">⚠️ 需要你确认</div>
      <div className="mb-3">{确认.摘要}</div>

      {确认.影响范围 && (
        <details className="mb-3 text-sm">
          <summary className="cursor-pointer">查看受影响的内容（{确认.影响范围.length} 项）</summary>
          <ul className="mt-2 space-y-1">
            {确认.影响范围.map((x, i) => <li key={i}>· {x}</li>)}
          </ul>
        </details>
      )}

      <div className="flex gap-2">
        <button onClick={() => 决定(true)} disabled={处理中}
                className="px-4 py-2 bg-amber-600 text-white rounded disabled:opacity-50">
          批准
        </button>
        <button onClick={() => 决定(false, "用户拒绝")} disabled={处理中}
                className="px-4 py-2 border rounded disabled:opacity-50">
          拒绝
        </button>
      </div>
    </div>
  );
}
```

### 三个要点

**一、显示「影响范围」而不是原始参数。** U4.4 讲过：用户看不懂 `ids=[201,205,...]`。

**二、按钮要有 disabled 状态。** 防止重复点击。

**三、拒绝时可以让用户填理由。** 这个理由会回填给模型（U4.4 的设计），让它调整策略。

---

## 步骤 6 · 打磨细节

### 这一步在做什么

让它像个成品。

### 清单

**Markdown 渲染**

```bash
pnpm add react-markdown remark-gfm
```

模型输出经常带 Markdown（列表、加粗、表格）。不渲染的话用户看到的是一堆 `**` 和 `|`。

**代码块高亮**：如果你的场景会输出代码，加一个高亮库。

**自动滚动**（U5.2 学的），但要加一个判断：**用户手动往上滚时不要强制拉回底部。**

```tsx
const [自动滚动, 设自动滚动] = useState(true);

const 处理滚动 = (e) => {
  const { scrollTop, scrollHeight, clientHeight } = e.currentTarget;
  设自动滚动(scrollHeight - scrollTop - clientHeight < 50);   // 接近底部才自动滚
};
```

**这个细节很影响体验。** 用户想回看前面的内容时，界面一直把他拽到底部会很烦。

**复制按钮**、**重新生成**、**停止**。

**错误提示**：区分「模型出错」「网络断了」「配额用完了」，给不同的提示和操作建议。

---

## 完成判据

**产出**：`s5-frontend/web/src/app/chat/`

**动手部分**

- [ ] SSE 转发路由，含鉴权、归属校验、`signal` 传递、防缓冲头
- [ ] 前端接收事件，**按 id 去重**
- [ ] **执行过程可视化**：每一步显示人话描述、状态图标、耗时
- [ ] 已完成的步骤折叠，可展开看详情
- [ ] **停止按钮**：前端断开的同时后端也真的取消
- [ ] **人工确认卡片**：显示摘要和影响范围，不是原始参数
- [ ] Markdown 渲染
- [ ] 智能自动滚动（用户上滚时不强制拉回）
- [ ] 错误状态区分处理

**关键验证 1：完整跑一次多步任务**

录一段屏（或者截几张图），展示从提交到完成的全过程，**能看到每一步在做什么**。

**关键验证 2：停止真的生效**

点停止后，去后端查任务状态，**确认变成了 cancelled**，而不是还在跑。

**关键验证 3：断线重连**

任务跑到一半，刷新页面，**确认能重新连上并看到中间的进度**（不是从头开始，也不是丢失中间的）。

**理解检查**（笔记里回答）

1. 为什么转发时要传 `signal: req.signal`？不传会怎样？
2. `X-Accel-Buffering: no` 解决什么问题？
3. EventSource 的最大优势是什么？它有哪两个限制？
4. 为什么工具调用要显示「人话描述」而不是原始参数？
5. 智能自动滚动的判断逻辑是什么？为什么需要它？
