# 扩展模块 B · 大厂平台基建（+36 小时，可激活）

**这个模块默认不开。** 它对应的是「大厂 Agent Infra / 平台基建岗」那个池子，和主线 A 的池子要求不同。

**为什么做成可激活的，而不是直接排进主线**：你要到 W32 之后才会真正投简历。现在就锁死路径，等于替 8 个月后的你做决定。这个模块把内容准备好、触发条件写清楚，你到时候拿数据自己开。

---

## 一、两个池子的差异（这是激活判断的依据）

本次核验了 84 条 2026 年在招 JD，拆开看它们**不是同一个池子**：

| 池子 | JD 条数 | Go / Java 要求 | 主线 A 的匹配度 |
|---|---|---|---|
| 海外 AI Engineer / FDE | 14 | **0 次** | 完全匹配 |
| AI 创业公司全栈 | 21 | 口径是「至少精通一门后端语言」，Python + TypeScript 已满足 | 完全匹配 |
| **大厂 Agent 工程师** | 26 | **Go 11 次 + Java 10 次** | **语言是硬门槛** |
| **通用后端（AI 方向）** | 10 | **9 条要 Java 或 Go** | **语言是硬门槛** |

**所以这不是能力缺口，是岗位池选择问题。**

35 条 JD 的池子（海外 + 创业）里，主线 A 的技术栈完全正确，而且 S3 和 S4 的产出在这个池子里是稀缺品。

36 条 JD 的池子（大厂 + 通用后端）里，不会 Go 或 Java 会在**筛简历阶段**就出局——不是面试挂，是根本进不去。

---

## 二、什么时候激活

**不要凭感觉决定。** 在这三个时间点用数据判断：

**判断点一 · W9（U1.9 需求发现做完时）**

那个单元会让你去接触真实的业务流程。做完后问自己：你更擅长、也更享受的是「把别人的模糊需求拆成规格并交付」，还是「把系统的某一层做深做稳」？

前者指向池子 A，后者指向池子 B。

**判断点二 · W20（M3 评估体系做完时）**

这时你手上有了 S3 那三样产出。**去实际搜 20 条当下在招的 JD**（不是看这份文档里的旧数据），按池子分类，统计：

| | 池子 A（海外 / 创业） | 池子 B（大厂 / 通用后端） |
|---|---|---|
| 我能投的岗位数 | | |
| 学历/年限门槛我过得了几条 | | |
| 语言要求我满足几条 | | |
| 我真的想去几家 | | |

**如果池子 B 的「我真的想去」超过池子 A，就激活这个模块。**

**判断点三 · W32（M5 做完，开始准备求职时）**

最后的窗口。这时激活的话，模块会排在 S6 之后，总周期变成 40 周左右。

---

## 三、模块内容（36 小时）

### B1 · Go 读懂级 · 16h

**目标不是「会写 Go」，是「看得懂、改得动、面试能聊」。**

大厂 Agent Infra 岗的现实是：核心链路多半是 Go 写的，你入职后第一件事是读它。JD 要的是这个，不是让你从零写一个 Go 服务。

**教什么**：

语法快速过一遍——包与模块、结构体与接口、错误处理（为什么是返回值不是异常）、defer。**重点在 Go 的并发模型**：goroutine 和 channel、`select`、`sync.WaitGroup` 与 `Mutex`、context 的取消传播。

然后做一件具体的事：**把你 S0 的 csvstats API 用 Go 重写一遍**。用 `net/http` 或 gin，接同一个 Postgres，跑同一套接口。**不追求写得地道，追求跑通并能解释每一处和 Python 版的差异。**

最后读一段真实的 Go 代码：挑一个你用过的 Go 项目（比如 Docker 或 Kubernetes 的某个模块），读 500 行，写一页笔记说明它的并发模型。

**判据**：能回答「Go 的 goroutine 和 Python 的 asyncio 有什么本质区别」——这道题的答案是「一个是抢占式调度的轻量线程，一个是协作式的单线程事件循环」，能展开讲的人不多。

### B2 · Kubernetes 读懂级 · 6h

**同样是读懂级，不碰集群搭建。**

本机用 kind 或 k3d 起单节点集群。五个对象讲清楚：Pod、Deployment、Service、ConfigMap 与 Secret、Ingress。

把 S4 已经打好的镜像部上去，做一次滚动更新和一次 `kubectl rollout undo`。**这里有个很好的对照点**：U4.6 已经用应用层配置做过灰度和回滚，这里是同一件事在基础设施层的做法，两边对照讲，印象最深。

排障演练：故意写错镜像名看 `ImagePullBackOff`，故意让 readiness 失败看流量怎么被摘掉（**U4.7 已经写好 `/ready` 端点，直接复用**），故意超内存看 `OOMKilled`。

`kubectl get / describe / logs / exec / port-forward` 五个命令组成的排障流程。

**明确不学**：集群搭建、CNI、Operator、Helm chart 开发。

### B3 · 微服务与分布式 · 8h

**先说清楚证据强度**：这一项只有「通用后端」一个角度提到（6/10），其余 74 条 JD **0 次提及**。所以它是池子 B 专属，不是通用要求。

**教什么**：服务拆分的边界怎么划（按业务能力还是按数据所有权）；服务间通信（同步 REST/gRPC vs 异步事件）；分布式事务为什么难，Saga 模式怎么做补偿；服务发现与配置中心解决什么问题；熔断、降级、限流三件套——**U4.5 已经做过成本熔断，这里是同一个思想在服务调用层的应用**。

动手：把你的 Agent 服务拆成两个（比如检索服务和编排服务），用 gRPC 通信，跑通一次跨服务的分布式追踪（**接 S3 已有的 Langfuse，trace 要能串起来**）。

**判据**：能说清「什么规模下拆微服务是负收益」——这和 U4.3 对消息队列、U6.4 对 A2A 的处理是同一个套路。

### B4 · 模型内部机制 · 6h

**为什么池子 B 需要而池子 A 不需要**：平台基建岗要做推理服务、要调吞吐、要判断什么模型能上什么卡，这些绕不开机制层面。

**教什么**：Transformer 的注意力机制（不推公式，讲清楚为什么计算量随序列长度平方增长）；KV cache 是什么、为什么它决定并发上限、为什么它是显存大头；PagedAttention 解决 KV cache 的内存碎片；continuous batching 为什么吞吐能高几倍；量化（Q4/Q8/AWQ/GPTQ）各自的取舍。

动手：租一张卡，用 vLLM 起一个 OpenAI 兼容端点，**直接接进 U1.2 已经写好的统一客户端——改一行配置就通，这本身就是那层抽象价值的最好证明**。压测出三条曲线：并发对吞吐、并发对首 token 延迟、并发对 P99。最后算一笔账：自建对比调 API，在多大调用量上打平。

**没有 GPU 的话**：降级为概念 + 读 vLLM 文档 + 只做成本测算那部分，压到 3 小时。

---

## 四、激活后的周次安排

在主线 36 周之后追加：

| 周 | 内容 |
|---|---|
| W37 | B1 Go 读懂级（16h 的前半） |
| W38 | B1 余 + B2 Kubernetes(6h) |
| W39 | B3 微服务(8h) |
| W40 | B4 模型内部机制(6h) + 作品集补充 |

**总周期 40 周。**

**如果在 W20 就决定激活**，更好的做法是把 B1 提前插到 S4 之前（Go 的并发模型会让你更容易理解 U4.3 的异步架构），其余三项保持在最后。

---

## 五、一条重要提醒

**激活这个模块的成本不只是 36 小时。**

真正的成本是：你把 36 小时投进「补齐池子 B 的门槛」，而不是投进「把池子 A 里的优势拉得更开」。

本次核验的一个明确结论是：**在池子 A 里，你的 S3 评估体系和 S4 生产化产出是稀缺品**——绝大多数三年经验的候选人拿不出 CI 拦截劣化 PR 的截图、拿不出完整的失败到评估题的闭环记录、拿不出 judge 校准报告。

**而在池子 B 里，你是「Python 转过来的、Go 刚入门的初级后端」。**

所以除非判断点二的数据明确指向 B，**默认不激活是更优解**。

---

## 六、不激活也要做的一件事：2 小时的池子定位

**这件事无论如何都要做，已经写进 U1.8 和 U6.6。**

它比 36 小时的 B 模块更值钱，因为它防止你拿着一份很强的 Agent 作品集，去投一个默认要求手写 Java 并发代码的岗位。

具体做法见 U6.6 的「步骤 0 · 简历与投递」。
