# 参与贡献

这份路线图最大的敌人是**过期**。版本号、官方文档链接、招聘要求、工具的命令行参数，每一样都会在几个月内变。所以最有价值的贡献不是加新内容，是**发现某处已经不对了，并附上证据**。

## 一条硬规则：附一手来源

任何涉及事实的改动——版本号、数字、某个工具「支持 / 不支持」什么、某条 JD 要求什么——都必须附可核验的一手来源：

| 类型 | 算一手来源 | 不算 |
|---|---|---|
| 包版本 | npm registry、PyPI、GitHub Releases 页面 | 博客、教程、AI 回答 |
| 工具行为 | 官方文档、源码、你自己复现的终端输出 | 「我记得」、别人的截图 |
| 招聘要求 | 招聘网站上能打开的 JD 原文，且发布时间在近 6 个月内 | 面经汇总、二手转述 |
| 安全问题 | NVD / GitHub Advisory 的 CVE 编号 | 新闻标题 |

**没有来源的事实性改动会被要求补充，补不上就不合并。** 这不是官僚，是这份文档从第一版开始就在执行的纪律——第一版草稿里 6 类看起来很像样的数据全是 SEO 农场编的，删掉之后才有了今天的版本。

## 三种贡献方式

**1. 报告过期或错误**：开 issue，用「信息过期 / 步骤跑不通」模板。写清楚哪个文件哪一节、你看到的和文档写的分别是什么、来源链接。

**2. 报告卡点**：某一步你卡了超过一小时、文档的「可能卡在哪」没覆盖，开 issue 用「卡点」模板。**这类 issue 会直接变成文档里的新一条「可能卡在哪」**，是对后来者最有用的贡献。

**3. 直接改**：Fork → 改 → PR。PR 描述里写清楚改了什么、为什么、来源是什么。

## 改动的边界

**欢迎**：修错、更新版本、补卡点、补验收标准、修链接、修错别字、改善某一步的可操作性。

**先开 issue 讨论再动手**：加新单元、调整周次和时长、改变技术栈选型、加新的学习路径。这些改动会牵连时长核算和前后依赖，需要先对齐。

**不接受**：把某个单元换成「去看 XX 课程」（这份文档的原则是自包含）；加没有验收标准的内容；加只有理论没有动手的章节；为了面面俱到加「了解即可」的清单。

## 写作规范

每个单元的每一步都按同一个结构：**这一步在做什么 / 怎么做 / 可能卡在哪 / 背后的逻辑**。改动要维持这个结构。

- 代码块里的标识符用英文，注释用中文
- 每段能跑的代码后面给出「你应该看到」的输出，**必须是你实际跑出来的**，不是你觉得应该是什么
- 所有路径用 `~/ai-agent-roadmap` 作为仓库位置，用 `you` 代表用户名
- 命令行按 macOS / Linux 写，Windows 的差异集中在 U0.1 开头的 WSL2 说明里，不要在各处散落 Windows 分支
- 数字（时长、次数、百分比）要么来自你能贴出来的来源，要么写「在你自己的环境里量一下」

## 时长核算

改动任何单元的时长，要同步单元标题、阶段 `units/sX/README.md`、根 `README.md` 和 `docs/周计划.md`。周计划中括号表示本周投入，同一个单元可跨周，累计必须等于标题课时；每周单元学习不超过 16h，复盘另留 4h。运行 `python3 scripts/check_curriculum.py` 检查目录、课时和排期是否一致。

## 本地预览网页版

```bash
pip install -r requirements-docs.txt
bash scripts/build_site.sh
python3 scripts/check_curriculum.py
mkdocs build --strict
mkdocs serve
```

打开 http://127.0.0.1:8000 。
